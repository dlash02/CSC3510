function addNToX( x, n ){
    // rule - must always return a pos number
    return Math.abs(x)+Math.abs(n);
}
function subNFromX( x, n ){
    // rule - must always return a pos number
    return Math.abs(x)-Math.abs(n);
}
