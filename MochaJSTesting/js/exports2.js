// can have export at bottom separately
function getName() {
    let name = prompt("What is your name->");
    return name;
}
function getAge() {
    let name = prompt("What is your age->");
    return name;
}
export { getName, getAge}
