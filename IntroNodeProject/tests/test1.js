const assert = require('assert');

describe( "File to be tested", () => {
    context("Describe function to be tested", () => {
        it("describe specific test", () => {
           assert.equal(1,1);
        });
    });
});