const assert = require('assert');

describe('file to be tested', function() => {
    context("function to be tested"), function() => {
        it( "should do something", function() => {
            asser.equal( 1, 1);

        })
    }
})